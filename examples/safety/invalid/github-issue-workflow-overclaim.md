The GitHub issue repair agent can merge feature-generated PRs automatically,
approve the PR review, and mark the branch ready for review. It can comment on
the issue and close or reopen issues after it decides a bug is fixed.

The compatibility gate is active, a new release is published, external beta launched,
provider pilot launched, promotion is granted, and RSI is authorized.
